# Server Configuration

Mini PC Ubuntu Server configuration.

## Features
- Static IP via Netplan
- SSH hardened (keys only)
- Firewall locked down
- Tailscale remote access
- Temperature monitoring + alerts
- Boot + uptime alerts via Discord

## Restore steps
1. Install Ubuntu Server
2. Copy netplan config
3. Install packages
4. Enable systemd services
5. Restore secrets manually
